<!-- SOURCE REMOVED (2026-07-23). The SchemaStudio.AIHelpers project no longer exists in the solution. Verified: no *.csproj outside obj/, no source *.cs (only leftover obj/ build artifacts under SchemaStudio.AIHelpers/obj/), the solution index reports 3 projects, and there are no remaining references to [FileVersion] / [AIFileContext] anywhere in the solution. This doc is intentionally blanked - do NOT regenerate content for a project that does not exist. If the attribute library is reintroduced, regenerate from the index. -->

# SchemaStudio.AIHelpers - REMOVED

The `SchemaStudio.AIHelpers` project (the `[FileVersion]` / `[AIFileContext]` attribute library, formerly
`AIAttributes.cs`) has been **removed from the solution**. There is no source left to document; only stale
`obj/` build artifacts remain on disk.

Solution map: [../Architecture.md](../Architecture.md).
