# TODO

- Verify repository error handling for `SchemaMCPRepository` and the MCP tool wrappers.
- Decide whether MCP failures should expose exception details outside development builds or collapse to stable error codes/messages only.
- Add deeper parser/sql-generation MCP tools after the catalog discovery chain is stable.
